#include <d3d12.h>
#include <RtxVideoSDK.h>

// Fixture signals for RTX Video SDK:
// ffmpeg decoded frame, video frame, frame surface, NV12, RGBA,
// 8-bit, 10-bit, bit depth, SDR, HDR, HDR10, color space,
// super resolution, artifact reduction, SDR-to-HDR, SDR to HDR,
// output surface, render target, present, display path.
void EnhanceVideoFrame(ID3D12Device* device) {
  (void)device;
}
