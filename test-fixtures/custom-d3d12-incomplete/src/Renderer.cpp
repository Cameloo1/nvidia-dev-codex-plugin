#include <d3d12.h>

void RenderFrame(ID3D12Device* device) {
  (void)device;
  // Intentionally incomplete fixture: native D3D12 shell only.
  // It does not expose the temporal contract required by DLSS readiness tests.
}
