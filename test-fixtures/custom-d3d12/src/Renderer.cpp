#include <d3d12.h>

// Fixture signals for classifier and patch planner:
// swapchain present, motion vectors, depth buffer, exposure, jitter, camera matrices.
void RenderFrame(ID3D12Device* device) {
  (void)device;
}
